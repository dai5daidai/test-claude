# test-claude

ブラウザで遊べるシンプルな **インベーダーゲーム** です。

## 遊び方

1. `index.html` をブラウザで開く
2. キー操作でプレイ
   - 移動: `←` `→` または `A` `D`
   - 発射: `Space`
   - リスタート: `R`（ゲームオーバー時）

## ローカル起動例

```bash
python3 -m http.server 8000
# http://localhost:8000 を開く
```
